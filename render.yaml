services:
  - type: web
    name: worksheet-builder
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
    healthCheckPath: /
